.root {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
}

.header {
}

@media (min-width: 768px) {
    :global(html.fullscreen) .header {
        padding-top: 64px;
    }
}

.main {
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
}
